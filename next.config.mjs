/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special config needed — zero-config Vercel deployment
  eslint: {
    ignoreDuringBuilds: true,
  },
}

export default nextConfig
